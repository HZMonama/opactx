# Example policy-facing fields merged into data.context.
env: dev

actor:
  id: user-123
  role: admin
  email: platform-admin@example.com

request:
  action: deploy
  resource:
    type: service
    id: payments-api
