# Minimal opactx pipeline configuration.
# Docs: https://opactx.gitbook.io/opactx/

version: v1

schema: schema/context.schema.yaml
context_dir: context

sources: []
transforms: []

output:
  dir: dist/bundle
  include_policy: false
  tarball: false
