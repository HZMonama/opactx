version: v1

schema: schema/context.schema.json
context_dir: context

sources: []

transforms:
  - name: canonicalize
    type: builtin
    with: {}

output:
  dir: dist/bundle
  include_policy: false
  tarball: false
